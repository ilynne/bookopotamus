%h1 Books
%h2= current_user.id if current_user.present?
%table
  %tr
    %th ISBN-10
    %th ISBN-13
    %th Title
    %th Author
    %th Rating
    - if current_user.present?
      %th My Rating
  - @books.each do |book|
    %tr
      %td= book.isbn_10
      %td= book.isbn_13
      %td= book.title
      %td= author_last_first(book)
      %td
        .star{:data => {'score' => book.average_rating}}
      - if current_user.present?
        %td
          .user_star{:data => {'score' => book.user_rating(current_user).score, 'rating-id' => book.user_rating(current_user).id}}
