#books_search_form
  = form_tag books_path, :method => :get do
    #books_search_form_searchbox.input-append
      = text_field_tag :search, params[:search], :placeholder => 'Search ...'
      %button.btn{:style => "margin-left: -6px;", :type => "submit", :id => 'search_button'}
        %i.icon-search.icon-white
