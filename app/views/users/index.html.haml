%h1 Users

- if @users.count > 0
  %h3= "#{@users.count} users found:" 
  %table.table
    %tr
      %th Restricted
      %th Email
      %th Admin
    - @users.each do |user|
      %tr
        %td= user.restricted
        %td= user.email
        %td= user.admin
  = will_paginate @users
- else
  %p No users! 
  = link_to 'Invite admin', new_book_path

