%h1 Reviews
%h2= @book.title
%table.table
  %tr
    %th= sortable 'score', 'Rating'
    %th= sortable 'title'
    %th Review
    %th Member
    - if current_user.present?
      %th My Rating
  - @reviews.each do |review|
    %tr
      %td
        = render 'shared/book_user_rated', :book => review.book, :user => review.user, :score => review.score
      %td= review.book.title
      %td= review.body
      %td= review.user.email
      - if current_user.present?
        %td
          = render 'shared/book_user_rating', :book => review.book, :user => current_user
= will_paginate @books


