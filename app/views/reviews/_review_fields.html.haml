.field
  - if f.object.user_id == current_user.id
    = f.text_area :body, :disabled => !@book.active
    = f.hidden_field :user_id, value: current_user.id
    = f.hidden_field :book_id, value: @book.id
